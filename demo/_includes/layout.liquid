<!DOCTYPE html>
<html lang="en=US" dir="ltr">
  <head>
    <meta charset="UTF-8">
    <title>Example (Liquid)</title>
    {% jsonLdScript meta type tags %}
  </head>
  <body>
    <h1>{{ meta.title }}</h1>
    {{ content }}
  </body>
</html>
